@model IEnumerable<EmployersSalary.Models.Employer>
@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Employers</h2>
<p>
    @Html.ActionLink("New Employer", "New", "Employers", null, new { @class = "btn btn-primary" })
</p>
<table id="employers" class="table table-bordered table-hover">
    <thead>
    <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Net Salary, $</th>
        <th>Delete</th>
    </tr>
    </thead>
    <tbody></tbody>
</table>

@*@section scripts
{
    <script>
        $(document).ready(function () {
            $("#employers .js-delete").on("click", function () {
                var button = $(this);

                if (confirm("Are you sure you want to delete this customer?")) {
                    $.ajax({
                        url: "/api/customers/" + button.attr("data-customer-id"),
                        method: "DELETE",
                        success: function() {
                            button.parents("tr").remove();
                        }
                    });
                }
            });
        });
    </script>
}*@

@section scripts
{
    <script>
        $(document).ready(function () {
            $("#employers").DataTable({
                ajax: {
                    url: "/api/employers",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "firstName"
                        ,
                        render: function(data, type, employer) {
                            return "<a href='/employer/edit/" + employer.id + "'>" + employer.firstName + "</a>";
                        }
                    }
                    ,
                    {
                        data: "lastName",
                        render: function (data, type, movie) {
                            return "<a href='/employer/edit/" + movie.id + "'>" + movie.lastName + "</a>";
                        }
                    },
                    {
                        data: "netSalary"
                    },
                    {
                        data: "id",
                        render: function(data) {
                            return "<button class='btn-link js-delete' data-movie-id=" + data + ">Delete</button>";
                        }
                    }
                ]
            });


            //$("#employers").on("click", ".js-delete", function () {
            //    var button = $(this);

            //    bootbox.confirm("Are you sure you want to delete this movie?", function (result) {
            //        if (result) {
            //            $.ajax({
            //                url: "/api/movies/" + button.attr("data-movie-id"),
            //                method: "DELETE",
            //                success: function () {
            //                    table.row(button.parents("tr")).remove().draw();
            //                }
            //            });
            //        }
            //    });
            //});
        });
    </script>
}
